package provider.model;

/**
 * Three possible states for a hex to be in. White means that
 * a hex is occupied by player white. Black means that a hex
 * is occupied by player black. Empty means that a hex is not
 * occupied by either player.
 */
public enum HexState {
  WHITE, BLACK, EMPTY
}
